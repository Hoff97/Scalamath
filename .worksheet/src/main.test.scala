package main

import scala.collection.mutable._

object test {;import org.scalaide.worksheet.runtime.library.WorksheetSupport._; def main(args: Array[String])=$execute{;$skip(106); 
	println("Welcome to the Scala worksheet");$skip(18); val res$0 = 
	"abc".slice(1,2);System.out.println("""res0: String = """ + $show(res$0))}
}
